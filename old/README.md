# Archived site

`legacy-site/` contains the former static IRAID HTML site, its legacy CSS, and the assets that supported it. The live site is the Next.js app in `app/`, `components/`, `data/`, and `public/`.
